{% extends 'base.html.twig' %}
{% block body %}
<div class="container containerForm">
    <div class="form">
    <h2>Add a category</h2>
    {{ form_start(form) }}
        {{form_row(form.Name,{'attr':{'class':'form corm-control'}})}}

        <input type="submit" value='Save' class='btn btn-primary mt-4'>
    {{form_end(form)}}
    </div>

</div>

{% endblock %}