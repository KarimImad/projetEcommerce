{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier</h1>

        {{ include('city/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_city_index') }}">Précedent</a>

        {{ include('city/_delete_form.html.twig') }}
    </div>
{% endblock %}
